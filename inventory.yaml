all: 
  children:
    ubuntu:
      hosts:
        ubuntu1:
          ansible_host:
          ansible_user: ansible
          ansible_password: ansible
          ansible_become: true
          ansible_become_password: ansible